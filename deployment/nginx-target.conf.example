# Target TLS listener template.
# Keep this listener private on loopback.

server {
    listen 127.0.0.1:8443 ssl proxy_protocol;
    server_name sl.eduwav.com;

    ssl_certificate /etc/ssl/sl.eduwav.com/fullchain.pem;
    ssl_certificate_key /etc/ssl/sl.eduwav.com/privkey.pem;

    proxy_set_header X-MSISDN $proxy_protocol_tlv_0xe0;

    location / {
        proxy_pass http://127.0.0.1:8080;
    }
}

server {
    listen 127.0.0.1:8443 ssl proxy_protocol;
    server_name sl.yumzyy.com;

    ssl_certificate /etc/ssl/sl.yumzyy.com/fullchain.pem;
    ssl_certificate_key /etc/ssl/sl.yumzyy.com/privkey.pem;

    proxy_set_header X-MSISDN $proxy_protocol_tlv_0xe0;

    location / {
        proxy_pass http://127.0.0.1:8080;
    }
}

server {
    listen 127.0.0.1:8443 ssl proxy_protocol;
    server_name sl.radiofyy.com;

    ssl_certificate /etc/ssl/sl.radiofyy.com/fullchain.pem;
    ssl_certificate_key /etc/ssl/sl.radiofyy.com/privkey.pem;

    proxy_set_header X-MSISDN $proxy_protocol_tlv_0xe0;

    location / {
        proxy_pass http://127.0.0.1:8080;
    }
}
